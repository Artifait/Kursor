<Window x:Class="KursorClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursorClient"
        mc:Ignorable="d"
 Title="CursorShare — MVP" Height="360" Width="520">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" >
            <Label Content="Mode:" VerticalAlignment="Center"/>
            <ComboBox x:Name="ModeCombo" Width="120" Margin="6,0" SelectedIndex="0">
                <ComboBoxItem>Teacher</ComboBoxItem>
                <ComboBoxItem>Student</ComboBoxItem>
            </ComboBox>

            <Label Content="Server URL:" VerticalAlignment="Center" Margin="12,0,0,0"/>
            <TextBox x:Name="ServerUrlBox" Width="220" Margin="6,0" Text="http://localhost:5254"/>

        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left: teacher area / student info -->
            <Border Grid.Column="0" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Padding="8">
                <StackPanel>
                    <!-- Teacher controls -->
                    <StackPanel x:Name="TeacherPanel">
                        <TextBlock FontWeight="Bold" Text="Teacher" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <Label Content="Password:" VerticalAlignment="Center"/>
                            <TextBox x:Name="TeacherPassword" Width="240" Margin="6,0" />
                        </StackPanel>
                        <WrapPanel>
                            <Button x:Name="CreateRoomBtn" Width="120" Click="CreateRoomBtn_Click">Create Room</Button>
                            <Button x:Name="StartTeacherBtn" Width="120" Margin="8,0" Click="StartTeacherBtn_Click">Start Sending</Button>
                            <Button x:Name="StopTeacherBtn" Width="120" Margin="8,0" Click="StopTeacherBtn_Click">Stop</Button>
                        </WrapPanel>

                        <TextBox x:Name="TeacherInfo" Margin="0,12,0,0" Text="Room: — | Token: —" TextWrapping="Wrap"/>
                        <TextBlock Margin="0,6,0,0" Text="Move the mouse inside this window area to control the shared cursor."/>
                    </StackPanel>

                    <!-- Student controls -->
                    <StackPanel x:Name="StudentPanel" Visibility="Collapsed">
                        <TextBlock FontWeight="Bold" Text="Student" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <Label Content="Room ID:" VerticalAlignment="Center"/>
                            <TextBox x:Name="StudentRoomId" Width="140" Margin="6,0"/>
                            <Label Content="Password:" VerticalAlignment="Center" Margin="6,0,0,0"/>
                            <TextBox x:Name="StudentPassword" Width="120" Margin="6,0"/>
                        </StackPanel>
                        <WrapPanel>
                            <Button x:Name="JoinRoomBtn" Width="120" Click="JoinRoomBtn_Click">Join Room</Button>
                            <Button x:Name="StartStudentBtn" Width="120" Margin="8,0" Click="StartStudentBtn_Click">Start Receiving</Button>
                            <Button x:Name="StopStudentBtn" Width="120" Margin="8,0" Click="StopStudentBtn_Click">Stop</Button>
                        </WrapPanel>
                        
                        <TextBlock x:Name="StudentInfo" Margin="0,12,0,0" Text="Token: —"/>
                        <!-- Новые строки: показываем полученные нормализованные координаты -->
                        <StackPanel Orientation="Vertical" Margin="0,6,0,0">
                            <TextBlock Text="Last received (normalized):" FontWeight="SemiBold" FontSize="12"/>
                            <TextBlock x:Name="ReceivedCoordsText" Text="x=—, y=—" FontFamily="Consolas" />
                            <TextBlock Text="Screen (physical / DIP / scale):" FontWeight="SemiBold" FontSize="12" Margin="0,8,0,0"/>
                            <TextBlock x:Name="ScreenInfoText" Text="—" FontFamily="Consolas" />
                        </StackPanel>
                    </StackPanel>

                </StackPanel>
            </Border>

            <!-- Right: capture area for teacher (preview) -->
            <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1" CornerRadius="4" Padding="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Capture area (Teacher):" FontWeight="Bold"/>
                    <Canvas x:Name="CaptureArea" Grid.Row="1" Background="WhiteSmoke" Margin="0,6,0,0"
                            MouseMove="CaptureArea_MouseMove"  Width="200" Height="200"
 />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>